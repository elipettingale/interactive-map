
@import "https://api.mapbox.com/mapbox-gl-js/v2.1.1/mapbox-gl.css";

:root {
    --primary-background: #d58449;
    --primary-color: #ffffff;
}

html, body {
    height: 100%;
    width: 100%;
    margin: 0;
    padding: 0;
}

.mapboxgl-map {
    height: 100vh;
    width: 100vw;
}

.btn {
    border: none;
    border-radius: 100%;
    height: 40px;
    width: 40px;
    background: var(--primary-background);
    color: var(--primary-color);
    cursor: pointer;
}

.app {

    &__controls {
        position: absolute;
        top: 0;
        left: 0;
        padding: 1rem;
    }

}
